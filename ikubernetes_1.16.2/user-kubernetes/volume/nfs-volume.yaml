apiVersion: v1
kind: Pod
metadata:
  name: nfs-volume-pod
  labels:
    app: redis
spec:
  nodeName: node1.k8s.io
  containers:
  - name: redis
    image: redis:alpine
    ports:
    - containerPort: 6379
      name: redisport
    volumeMounts:
    - mountPath: /data
      name: redisdata
  volumes:
    - name: redisdata
      nfs:
        server: nfs.ikubernetes.io
        path: /data/redis
